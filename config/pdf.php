<?php

return [

    /*
    |--------------------------------------------------------------------------
    | PDF generation queue (SRS §21.1)
    |--------------------------------------------------------------------------
    */
    'queue' => env('PDF_GENERATION_QUEUE', 'pdf-generation'),

    /*
    |--------------------------------------------------------------------------
    | Signed download URL expiry in hours (SRS §21.13)
    |--------------------------------------------------------------------------
    */
    'signed_url_hours' => (int) env('PDF_SIGNED_URL_HOURS', 24),

    /*
    |--------------------------------------------------------------------------
    | Payslip password protection using employee DOB DDMMYYYY (SRS §21.7)
    |--------------------------------------------------------------------------
    */
    'payslip_password_enabled' => (bool) env('PDF_PAYSLIP_PASSWORD_ENABLED', false),

    /*
    |--------------------------------------------------------------------------
    | Storage disk for generated PDF files
    |--------------------------------------------------------------------------
    */
    'disk' => env('PDF_STORAGE_DISK', 'local'),

    /*
    |--------------------------------------------------------------------------
    | Base path under disk root (SRS: storage/app/documents/)
    |--------------------------------------------------------------------------
    */
    'storage_path' => 'documents',

];
